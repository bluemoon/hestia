import all
from nose.tools import assert_equal
from nose import SkipTest

class TestNullPluginManager:
    def test___init__(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        raise SkipTest # TODO: implement your test here

    def test___iter__(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.__iter__())
        raise SkipTest # TODO: implement your test here

    def test_addPlugin(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.addPlugin(plug))
        raise SkipTest # TODO: implement your test here

    def test_addPlugins(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.addPlugins(plugins))
        raise SkipTest # TODO: implement your test here

    def test_delPlugin(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.delPlugin(plug))
        raise SkipTest # TODO: implement your test here

    def test_delPlugins(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.delPlugins(plugins))
        raise SkipTest # TODO: implement your test here

    def test_getPlugins(self):
        # null_plugin_manager = NullPluginManager(plugins, config)
        # assert_equal(expected, null_plugin_manager.getPlugins(name, interface))
        raise SkipTest # TODO: implement your test here

