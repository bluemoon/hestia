import all
from nose.tools import assert_equal
from nose import SkipTest

class TestThreadingPattern:
    def test___init__(self):
        # threading_pattern = threading_pattern(child, parent)
        raise SkipTest # TODO: implement your test here

    def test_hook(self):
        # threading_pattern = threading_pattern(child, parent)
        # assert_equal(expected, threading_pattern.hook())
        raise SkipTest # TODO: implement your test here

    def test_main(self):
        # threading_pattern = threading_pattern(child, parent)
        # assert_equal(expected, threading_pattern.main())
        raise SkipTest # TODO: implement your test here

    def test_run(self):
        # threading_pattern = threading_pattern(child, parent)
        # assert_equal(expected, threading_pattern.run(lock, child_connection, parent_queue))
        raise SkipTest # TODO: implement your test here

    def test_run_hook(self):
        # threading_pattern = threading_pattern(child, parent)
        # assert_equal(expected, threading_pattern.run_hook(*args, **kwargs))
        raise SkipTest # TODO: implement your test here

